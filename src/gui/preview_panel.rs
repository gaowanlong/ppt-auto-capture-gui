//! Preview Panel — shows the live capture preview.
//! Integrated into DashboardPanel for simplicity in this version.

// Preview functionality is handled inline in dashboard.rs
// This module exists for structural completeness and future expansion.

pub struct PreviewPanel;

impl PreviewPanel {
    pub fn new() -> Self {
        Self
    }
}
